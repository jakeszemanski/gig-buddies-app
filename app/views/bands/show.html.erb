<h1>Band name: <%= @band.name %></h1>
  <% @band.concerts.each do |concert| %>
    <h2>Shows: <%= concert.venue.name %></h3>
    <h3>Date: <%= concert.date %></h3>
    <h3>Doors: <%= concert.doors %></h3>
    <h3>Show: <%= concert.show %></h3>
    <% end %>